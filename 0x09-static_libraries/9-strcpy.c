#include "main.h"

/**
 * _strcpy - function
 * @dest: input
 * @src: input
 * Return: character
 */

char *_strcpy(char *dest, char *src)
{
	char *new_ptr1, *new_ptr2;

	new_ptr1 = dest;
	new_ptr2 = src;

	return (new_ptr1);
}
